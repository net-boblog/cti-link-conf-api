<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration 
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <settings>  
    	<!-- 启用mybatis二级缓存,全局开关：默认是true，如果它配成false，其余各个Mapper XML文件配成支持cache也没用 -->
    	<setting name="cacheEnabled" value="true"/>   
    	 <!-- 全局启用或禁用延迟加载。当禁用时，所有关联对象都会即时加载 -->  
    	<setting name="lazyLoadingEnabled" value="true"/> 
	</settings> 

    <!-- 命名空间别名 -->
    <typeAliases>
         <!--<typeAlias alias="Account" type="com.tinet.bossapi.model.Account"/> -->
    </typeAliases>
    
    <!-- 注册拦截器,实现postgresql数据库的物理分页 -->
    <plugins>
       <plugin interceptor="com.tinet.ctilink.core.dialect.limit.OffsetLimitInterceptor">
           <property name="dialectName" value="postgresql"/>
       </plugin>
    </plugins>
    
    <!-- 映射map -->
    <mappers>
    </mappers>
   
</configuration>